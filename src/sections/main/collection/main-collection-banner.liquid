{% schema %}
  {
    "name": "t:sections.main.collection_banner.name",
    "settings": [
      {
        "type": "checkbox",
        "id": "show_description",
        "label": "t:sections.main.collection_banner.settings.show_description.label"
      },
      {
        "type": "checkbox",
        "id": "show_image",
        "label": "t:sections.main.collection_banner.settings.show_image.label"
      }
    ]
  }
{% endschema %}